## Terraform Migration Plan (0.12 → 1.x)

- Upgrade Terraform version to 1.x
- Update provider plugins (AWS, etc.)
- Replace deprecated syntax
- Refactor monolithic files into modules
- Run terraform fmt and validate